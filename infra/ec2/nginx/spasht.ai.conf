# spasht.ai — static SPA (Cloudflare proxied, origin TLS via Cloudflare cert)
server {
    listen 80;
    listen [::]:80;
    server_name spasht.ai www.spasht.ai;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name spasht.ai www.spasht.ai;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;

    ssl_certificate     /etc/ssl/cloudflare/spasht.ai.pem;
    ssl_certificate_key /etc/ssl/cloudflare/spasht.ai.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    root /var/www/spashtai;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    location / {
        try_files $uri $uri/ @spa;
    }

    # Named fallback so add_header applies when serving index.html (try_files URI
    # fallback can skip headers from the parent location block).
    location @spa {
        add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
        rewrite ^ /index.html break;
    }

    location = /index.html {
        add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
