/**
 * Shared conversation color language across SpashtAI transcripts (Coach, Elevate).
 *
 * Blue = what the user said. Grey = what the coach said. Navy primary buttons are
 * reserved for actions and green for achieved states, so those two stay out of the
 * bubbles entirely.
 */
export const USER_BUBBLE = 'bg-blue-600 text-white'
export const COACH_BUBBLE = 'border bg-muted text-foreground'
